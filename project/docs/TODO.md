# TODO

- [ ] Добавить ежедневный `cron`-backup на мастере (например, `04:15`) с логом в `/var/log/hexenvpn-backup.log`.
- [ ] Опционально добавить команду бота `/capacity` (только для админа) поверх `project/scripts/capacity_estimate.sh`.
- [ ] UX для пользователей:
  - кнопка `📱 Инструкция` в главном меню (iOS/Android кратко).
  - улучшить `👤 Моя подписка`: показывать осталось дней + напоминания за 3/1 день до окончания.
  - кнопка `🛟 Не работает` с коротким чеклистом и переходом в поддержку.
  - мини-onboarding после `/start`: 1) получить ссылку, 2) выбрать приложение.
- [ ] UX для админа:
  - раздел `📌 Быстрые действия` (тестовый юзер на 1 день, продлить +30, ручной sync).
  - `👀 Инцидент-режим`: одной кнопкой `healthcheck + smoke + metrics` и отчёт в чат.
  - карточка пользователя: срок/статус/наличие на узлах + быстрые действия.
  - журнал админ-действий в SQLite (`add/edit/block/unblock/del`).
- [ ] Базовый ops-контур по репликам (UK/TR) отдельно:
  - отдельные healthcheck-скрипты/режимы для каждого узла (`uk`, `tr`) с явным статусом.
  - проверка ключевых параметров реплики: `xray active`, порт `443`, число клиентов, дубли в `xray config`.
  - быстрые операционные команды по узлу: диагностика, мягкий рестарт `xray`, проверка после рестарта.
  - отдельные алерты в Telegram по конкретной реплике (не только общий master+replicas).
  - короткий runbook «упала реплика UK/TR» с шагами восстановления.
- [ ] Архитектура v2 при росте нагрузки:
  - текущий режим (`xray + bot + subscription` на мастере) оставить как baseline.
  - подготовить план выноса `bot + web/subscription` на отдельную VM.
  - мастер оставить как control/data узел для `xray` + синхронизации.
  - сделать runbook миграции без даунтайма и rollback-план.
- [ ] Аналитика и наблюдаемость (master + UK/TR):
  - учет устройств пользователя (если клиент передает данные): `first_seen`, `last_seen`, `user_agent`, `ip`, `hwid/device_id`.
  - команда в админке `Устройства пользователя`: список устройств + количество активных/всего.
  - онлайн/офлайн статус пользователя: активные сессии сейчас + время последней активности.
  - учет трафика по пользователю (`uplink/downlink`) через `xray stats/api` с записью в SQLite.
  - общий трафик по узлу и по всем узлам (дневной/месячный) с отправкой отчета в Telegram.
  - алерты по аномалиям: резкий рост трафика, всплеск подключений, долгий офлайн узла.
- [ ] Дополнительные фичи (после стабилизации):
  - лимит одновременных устройств на пользователя (soft/hard режим + уведомления).
  - self-service для пользователя: `Сменить устройство`/`Сбросить активные сессии`.
  - антишеринг: детект частой смены гео/ip за короткое время и флаг в админке.
  - тарифы с автопродлением и напоминаниями до списания.
  - экспорт отчетов для админа (`CSV`/`JSON`): пользователи, активность, трафик, оплаты.
