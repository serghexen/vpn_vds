# TODO

- [ ] Добавить ежедневный `cron`-backup на мастере (например, `04:15`) с логом в `/var/log/hexenvpn-backup.log`.
- [ ] Опционально добавить команду бота `/capacity` (только для админа) поверх `project/scripts/capacity_estimate.sh`.
- [ ] UX (пользователь): кнопка `📱 Инструкция` в главном меню (iOS/Android кратко).
- [ ] UX (пользователь): улучшить `👤 Моя подписка` (осталось дней + напоминания за 3/1 день).
- [ ] UX (пользователь): кнопка `🛟 Не работает` с чеклистом и переходом в поддержку.
- [ ] UX (пользователь): мини-onboarding после `/start` (получить ссылку -> выбрать приложение).
- [ ] UX (админ): раздел `📌 Быстрые действия` (тестовый юзер 1 день, продлить +30, ручной sync).
- [ ] UX (админ): `👀 Инцидент-режим` одной кнопкой (`healthcheck + smoke + metrics`) и отчет в чат.
- [ ] UX (админ): карточка пользователя (срок/статус/узлы + быстрые действия).
- [ ] UX (админ): журнал админ-действий в SQLite (`add/edit/block/unblock/del`).
- [ ] Миграция реплик на Docker Compose:
- [x] Миграция реплик: подготовить `docker-compose` профиль для ноды (xray + volumes + restart policy).
- [x] Миграция реплик: безопасный runbook поэтапного перевода UK (backup -> precheck -> switch -> postcheck -> rollback).
- [ ] Миграция реплик: повторить на TR после стабилизации UK.
- [ ] Миграция реплик: проверить совместимость `vless-*`/sync-скриптов при docker-runtime на репликах.
- [ ] Архитектура v2: оставить текущий режим (`xray + bot + subscription` на мастере) как baseline.
- [ ] Архитектура v2: подготовить план выноса `bot + web/subscription` на отдельную VM.
- [ ] Архитектура v2: оставить мастер как control/data узел для `xray` + синхронизации.
- [ ] Архитектура v2: сделать runbook миграции без даунтайма и rollback-план.
- [ ] Доп.фича: лимит одновременных устройств на пользователя (soft/hard + уведомления).
- [ ] Доп.фича: self-service для пользователя (`Сменить устройство`/`Сбросить активные сессии`).
- [ ] Доп.фича: антишеринг (частая смена гео/ip за короткое время + флаг в админке).
- [ ] Доп.фича: тарифы с автопродлением и напоминаниями до списания.
- [ ] Доп.фича: экспорт отчетов для админа (`CSV`/`JSON`: пользователи, активность, трафик, оплаты).
